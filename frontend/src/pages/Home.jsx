import React from "react";

const Home = () => {
    return (
        <div>
            <h2>Welcome to the Vehicle Service Management System</h2>
            <p>Use the navigation bar to manage components, vehicles, issues, and services.</p>
        </div>
    );
};

export default Home;
